{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "JSXAttribute[name.name='dangerouslySetInnerHTML']",
        "message": "Use safeHtml() from lib/sanitize.ts instead of dangerouslySetInnerHTML"
      }
    ]
  }
}
